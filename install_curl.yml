---
- name: Install curl on Alpine Linux
  hosts: 192.168.64.130
  collections:
    - community.general
  tasks:
    - name: Update the package repository cache
      apk:
        update_cache: yes

    - name: Install curl
      apk:
        name: curl
        state: present
